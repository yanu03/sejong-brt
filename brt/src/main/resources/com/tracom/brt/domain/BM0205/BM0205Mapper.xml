<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tracom.brt.domain.BM0205.BM0205Mapper">
	<select id="BM0205G0S0" parameterType="String" resultType="vhcDvcUpdateVO">
		SELECT
			 V.VHC_NO
		   , DL4.DL_CD_NM AS VHC_KIND
		   , DL5.DL_CD_NM AS VHC_TYPE
		   , DL0.DL_CD_NM AS MAKER
		   , DL1.DL_CD_NM AS DVC_KIND
		   , DL3.DL_CD_NM AS INST_LOC
		   , D.MAKER
		   , D.DVC_ID
		   , D.MNG_ID
		   , DH.MODEL_NM
		   , DP.RSV_DATE
		FROM
		    BM_VHC_INFO V
			 LEFT OUTER JOIN BM_DVC_INFO D
			 ON V.VHC_ID = D.VHC_ID
			 LEFT OUTER JOIN BM_DVC_HIST DH
			 ON D.DVC_ID = DH.DVC_ID
			 LEFT OUTER JOIN BM_UPD_RSV DP
			 ON D.MNG_ID = DP.MNG_ID
			 LEFT OUTER JOIN (SELECT * FROM SM_DL_CD_INFO WHERE CO_CD = 'MAKER') DL0			 
			 ON D.MAKER = DL0.DL_CD	 
			 LEFT OUTER JOIN (SELECT * FROM SM_DL_CD_INFO WHERE CO_CD = 'DVC_KIND') DL1
			 ON D.DVC_KIND = DL1.DL_CD
			 LEFT OUTER JOIN (SELECT * FROM SM_DL_CD_INFO WHERE CO_CD = 'INST_LOC') DL3
			 ON D.INST_LOC = DL3.DL_CD
			 LEFT OUTER JOIN (SELECT * FROM SM_DL_CD_INFO WHERE CO_CD = 'VHC_KIND') DL4
			 ON V.VHC_KIND = DL4.DL_CD
			 LEFT OUTER JOIN (SELECT * FROM SM_DL_CD_INFO WHERE CO_CD = 'VHC_TYPE') DL5
			 ON V.VHC_TYPE = DL5.DL_CD
	   <where>
	    	<if test="value != null">
				DH.MODEL_NM LIKE CONCAT('%', #{value}, '%')
				OR V.VHC_NO LIKE CONCAT('%', #{value}, '%')
	    	</if>
    	</where>
    	GROUP BY DVC_ID
		ORDER BY VHC_NO 
	</select>
	
	<select id="BM0205G0S1" parameterType="vhcDvcUpdateVO" resultType="vhcDvcUpdateVO">
		SELECT
		   RSV_ID
		 , MNG_ID
		 , COMPLETE_YN
		FROM
			BM_UPD_LOG
		WHERE
			COMPLETE_YN = "N"
	</select>
	
	<insert id="BM0205Reservation" parameterType="vhcDvcUpdateVO">
		<selectKey keyProperty="rsvId,mngId" resultType="VhcDvcUpdateVO" order="AFTER">
    		SELECT 'key1' as rsvId , 'key2' as mngId FROM DUAL
    	</selectKey>	
		
		INSERT INTO BM_UPD_RSV(
			  RSV_DATE
			, RSV_ID
			, ATT_FILE
			, MNG_ID
			, CREATED_AT
    		, CREATED_BY
    		, CREATED_IP
    		, UPDATED_AT
    		, UPDATED_BY
    		, UPDATED_IP)
		VALUES
		<foreach collection="upList" item="upList" separator=",">
			( #{rsvDate}
		    , CONCAT('UR', LPAD(NEXTVAL(SEQ_BM_UPD_RSV_0), 5, '0'))
		    , #{upList.attFile}
		    , #{upList.mngId}
		    , #{upList.createdAt}
   			, #{upList.createdBy}
   			, #{upList.createdIp}
   			, #{upList.updatedAt}
   			, #{upList.updatedBy}
   			, #{upList.updatedIp}
		)
		</foreach>
	</insert>		
</mapper>