<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tracom.brt.domain.BM0302.BM0302Mapper">

    <select id="BM0302G0S0" parameterType="String" resultType="altContractInfoVO">
    	SELECT
    		*
    	FROM
    		BM_CON_CON
    	<where>
	    	<if test="value != null">
				CON_NO LIKE CONCAT('%', #{value}, '%')
				OR CON_NM LIKE CONCAT('%', #{value}, '%')
	    	</if>
    	</where>
    </select>

    <insert id="BM0302F0I0" parameterType="altContractInfoVO">
    	<selectKey keyProperty="conId" resultType="String" order="BEFORE">
    		SELECT CONCAT('CN', LPAD(NEXTVAL(SEQ_BM_CON_INFO_0), 5, '0'))
    	</selectKey>
    
    	INSERT INTO BM_CON_INFO(
			  CON_ID
    		, CON_NM
    		, CON_FST_DATE	
    		, CON_ST_DATE			
    		, CON_ED_DATE			
    		, CONFIRM_YN
    		, SUPP_AMT				
    		, VAT_AMT				
    		, CUST_ID				
    		, REMARK			
    		, CON_NO
    		)
    					
   		VALUES( 		
   			  #{conId}
   			, #{conNm}
   			, #{conFstDate}
   			, #{conStDate}
   			, #{conEdDate}
   			, "N"
   			, #{suppAmt}
   			, #{vatAmt}
   			, #{custId}
   			, #{remark}
   			, #{conNo}
   			
   		)
    </insert>
    
    <update id="BM0302F0U0" parameterType="altContractInfoVO">
    	UPDATE BM_CON_INFO
    	SET
    		  CON_NM			=	#{conNm}
    		, CON_FST_DATE		=	#{conFstDate}
    		, CON_ST_DATE		=	#{conStDate}
    		, CON_ED_DATE		=	#{conEdDate}
    		, CONFIRM_YN		=	#{confirmYn}
    		, SUPP_AMT			=	#{suppAmt}
    		, VAT_AMT			=	#{vatAmt}
    		, CUST_ID			=	#{custId}
    		, REMARK			=	#{remark}
    		, CON_NO			=	#{conNo}
    	
    	WHERE
    		CON_ID = #{conId}
    </update>
    
    <update id="BM0302F0U1" parameterType="altContractInfoVO">
    
    	UPDATE BM_CON_INFO
    	SET
    		  CON_NM			=	#{conNm}
    		, CON_FST_DATE		=	#{conFstDate}
    		, CON_ST_DATE		=	#{conStDate}
    		, CON_ED_DATE		=	#{conEdDate}
    		, CONFIRM_YN		=	"Y"
    		, SUPP_AMT			=	#{suppAmt}
    		, VAT_AMT			=	#{vatAmt}
    		, CUST_ID			=	#{custId}
    		, REMARK			=	#{remark}
    		, CON_NO			=	#{conNo}
    	
    	WHERE
    		CON_ID = #{conId}
    
    
    </update>
    
    <delete id="BM0302G0D0" parameterType="altContractInfoVO">
    	DELETE
    		FROM BM_CON_INFO
    	WHERE
    		CON_ID = #{conId}
    </delete>
</mapper>